# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- Config / validation ---
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0

# --- HTTP client (GitHub + Slack) ---
httpx>=0.27,<1.0

# --- AI (Gemini) ---
google-genai>=1.0.0

# --- Vector store (runbook semantic search) ---
chromadb>=0.5.0,<2.0

# --- Runbook PDF text extraction ---
pypdf>=5.0,<7.0
python-multipart>=0.0.9,<1.0
